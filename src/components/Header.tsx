import styles from '../App.module.css';

export const Header = () => {
    return (
        <div className={styles.headerMain}>
            <header>
                <span>IMC</span>
            </header>
        </div>
    );
};